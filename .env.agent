# Agent binary build mode — loaded by `vite build --mode agent`.
# This flag is baked into the bundle at compile time, not read at runtime.
# When set, ClerkProvider is excluded from the bundle entirely via dead-code
# elimination. Auth is cloud-only; the agent binary never initializes Clerk.
VITE_BUILD_TARGET=agent
