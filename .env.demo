# Demo build mode — loaded by `vite build --mode demo`.
# Baked in at compile time. The demo bundle has no Clerk, no backend: a fetch
# shim serves synthetic fixtures for /api/* and FleetStreamContext runs a fake
# EventSource. Deployed at demo.wicklee.dev and as a Hugging Face static Space.
VITE_BUILD_TARGET=demo
