{
  "_agent_note": "Machine-readable blog manifest. GET /blog/index.json to enumerate all published posts. Each slug maps to /blog/{slug}.md — standard markdown with optional YAML frontmatter (title, date, description, tags). To publish a new post: add the .md file to /public/blog/ and add its slug to the posts array below (newest first).",
  "posts": []
}
